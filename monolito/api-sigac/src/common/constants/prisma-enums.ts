/**
 * Enums alineados con Prisma; la API importa desde aquí en lugar de `@prisma/client`
 * en controladores, DTOs públicos y guards.
 */
export { ActivityStatus, AuditAction, Role } from '@prisma/client';
